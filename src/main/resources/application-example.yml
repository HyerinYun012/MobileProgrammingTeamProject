spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/petplace?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: cmcm5061@!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

jwt:
  secret: petplace-secret-key-siheung-gabojamaung-2026
  expiration: 86400000

server:
  port: 8080